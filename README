## Git Configuration

This repository uses a standard git commit message format. The template can be found in the .gitmessage file. To automatically use this template when making a commit, run:

```bash
git config --local include.path ../.gitconfig
```